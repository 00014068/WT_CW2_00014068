extends layout.pug 

block content
    .container 
        h1.title All notes

        each note in notes 
            .note-wrapper 
                a.note(href="/notes/detail")= note 
                a.featured.delete(href="#") Delete